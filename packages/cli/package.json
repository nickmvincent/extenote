{
  "name": "@extenote/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "extenote": "dist/index.js"
  },
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@extenote/core": "workspace:*",
    "@clack/prompts": "^0.7.0",
    "commander": "^12.1.0",
    "picocolors": "^1.0.0"
  }
}
